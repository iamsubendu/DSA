#include <iostream>
// for input/output
using namespace std;
// standard namespace

int main()
{

    cout << "Hello";
    return 0;
}
// std::cout
// without using namespace std, we have to write this everytime

// our code runs with 2 stages

// code.cpp(source code) -> compiler(checks whether everything is according to rule or not)
// compiler -> code.exe(executable file)
// 0/1 binary ->  executable file which will have binary instructions

// to compile(use powershell) -> g++ fileName.cpp -o executableFileName
// g++ command is a GNU c++ compiler invocation command, which is used for preprocessing,
// compilation, assembly and linking of source code to generate an executable file.

// to run exe -> ./filename.exe
